#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

	//long int num = 1000;
	///*sizeof("%zu/n", num);
	//sizeof("%zu/n", (int);*/
	//printf("%zu\n", sizeof num );
	//printf("%zu\n", sizeof(int));



	/*int length, volume;课堂试验，如何设计子函数
	printf("Please enter the length of a cube:");
	scanf_s("%d\n", &length);
	volume = power(length);
	int power(int y) {
		power(int y) = y * y * y;
	}
	printf("The volume of the cube is: %d\n", volume



	//int i, j;
	//float x, y;
	//i = 5;
	//j = 6;
	//x = 4.7f;
	//y = 3.5f;
	//printf("i=%d,j=%d,x=%f,y=%f\n", i, j, x, y);//question:为什么打印的结果是带多位小数的？
	//return 0;



	printf("*\t*\t*\n\t*\t*\n\t*");/*星号存在对齐问题*/



	/*输入输出基础
	int length, volume;
	printf("Please enter a length of a cube:");
	scanf("%d", &length);
	volume = length * length * length;
	printf("The volume of th cube is: %d\n", volume);*/



	/*第一个子函数
	int power(int x, int y);
	printf("6 power 3 equals %d\n", power(6, 3));
	printf("2 power 4 equals %d\n", power(2, 4));
	return 0;
}
	
	int power(int x,int y){
		int result=1;
		while(y > 0){
			result = result * x;
			y = y - 1;
		}
		return result;
}*/


//第一个输入输出与子函数结合的程序（第一，要记住多数据输入时如果scanf函数中间要使用逗号分隔，则控制台输入时也必须用英文逗号。
int main() {
	int power(int x, int y);

	int a, b;
	printf("please enter two integers to name a and its power:");
	scanf("%d,%d", &a, &b);
	printf("a powers b equals %d\n", power(a,b));
	return 0;
}
int power(int x, int y) {
	int result = 1;
	while (y > 0) {
		result = result * x;
		y = y - 1;
	}
	return result;
}




//多数据输入输出（要注意scanf函数后的\n会有冲突，而多数据之间间隔可以用逗号也可以简单空格）
//int main(void) {
//	int a, b;
//	printf("please enter two integers:");
//	scanf("%d %d",&a,&b);
//	printf("%d\t%d\n",a,b);
//	return 0;
//}
